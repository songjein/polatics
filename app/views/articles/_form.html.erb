<style>
	.emotion {
		width:50px;	
		height:50px;	
		background: rgba(250,250,250,0.3);
	}
</style>
<%= form_for @article do |f| %>
	<% if @article.errors.any? %>
		<div id="error_exaplanation">
			<h2>
				<%= pluralize(@article.errors.count, "error") %> prohibited
				this article from being saved:
			</h2>
			<ul>
				<% @article.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group">
		<%= f.label :제목, class: 'form-label' %></br>
		<%= f.text_field :title, class: 'form-input' %>
	</div>

	<div class="form-group">
		<%= f.label :내용, class: 'form-label' %></br>
		<%= f.text_area :text, class: 'form-input'%>
	</div>
	
	<figure class="avatar avatar-xl">
		<%= image_tag 'happy.png', class: 'emotion' %>
	</figure>
	<figure class="avatar avatar-xl">
		<%= image_tag 'soso.png', class: 'emotion' %>
	</figure>
	<figure class="avatar avatar-xl">
		<%= image_tag 'sad.png', class: 'emotion' %>
	</figure>
	<figure class="avatar avatar-xl">
		<%= image_tag 'angry.png', class: 'emotion' %>
	</figure>
	<figure class="avatar avatar-xl">
		<%= image_tag 'hard.png', class: 'emotion' %>
	</figure>

	<p>
	 개선사항)글쓰기 타입이 코드라면, 언어 선택 메뉴 표시
	</p>

	<p>
	 개선사항)글쓰기 타입이 독서라면, 책 이름 및 페이지 작성 폼 표시
	</p>

	<p>
	 개선사항)감정 3~5가지 선택(옵션- default: soso)
	</p>

	<p>
		<%= f.submit class: 'btn btn-block' %>
		<div class="loading"></div>
	</p>
<% end %>


